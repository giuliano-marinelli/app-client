import { Component, Input } from '@angular/core';
import { MatButtonModule } from '@angular/material/button';
import { MatCardModule } from '@angular/material/card';
import { MatDividerModule } from '@angular/material/divider';
import { MatIconModule } from '@angular/material/icon';

import { TranslocoModule } from '@jsverse/transloco';

import { {{properCase entity.name}} } from '../../../entities/{{dashCase entity.name}}.entity';

@Component({
  selector: '{{dashCase entity.name}}-mini',
  templateUrl: './{{dashCase entity.name}}-mini.component.html',
  imports: [
    MatButtonModule,
    MatCardModule,
    MatDividerModule,
    MatIconModule,
    TranslocoModule
  ]
})
export class {{properCase entity.name}}MiniComponent {
  @Input() {{camelCase entity.name}}!: {{properCase entity.name}};
}
